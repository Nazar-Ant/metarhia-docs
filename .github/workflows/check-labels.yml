name: Check Labels
on:
  pull_request:
    types:
      - opened
      - synchronize
      - labeled
      - unlabeled
      - reopened
jobs:
  check-labels:
    runs-on: ubuntu-latest
    steps:
      - name: Check Labels
        run: |
          ls -laR "${GITHUB_WORKSPACE}"
        env:
          LABELS: ${{ toJSON(github.event.pull_request.labels.*.name) }}
